
Intermediate


Extract name property from an array of objects.
Increase the age of all users by 1.
Convert an array of numbers into an array of booleans (even = true).
Round off an array of decimal numbers.
Format an array of dates as "DD/MM/YYYY".
Append “-done” to each task in a task array.
Extract domain names from an array of email addresses.
Replace all spaces in an array of strings with underscores.
Multiply each number by its index.
Remove vowels from an array of strings.
Advanced
Convert an array of objects {first,last} into {fullName}.
Convert nested arrays of numbers to strings.
Create an array of objects {number, square} from numbers.
Format an array of prices as currency strings.
Map an array of strings to objects with {original, length}.
Extract all numbers divisible by 3 from an array of arrays.
Capitalize the first letter of each string in an array.
Convert an array of booleans to words (true → "Yes").
Map an array of sentences to the number of words in each.
Map a list of students to their grade letters based on score.
31–50. (Create similar variations combining strings, numbers, arrays of objects, nested arrays, and calculations)
🟡 FILTER – 50 Practice Questions
Beginner
Filter numbers greater than 10.
Filter strings longer than 5 characters.
Keep only even numbers.
Keep only odd numbers.
Remove null values from an array.
Remove undefined values from an array.
Filter positive numbers.
Filter negative numbers.
Keep strings that contain “a”.
Remove empty strings.
Intermediate
Filter users older than 18.
Keep products with price < 100.
Filter out duplicate numbers.
Keep names starting with “A”.
Keep words ending with “ing”.
Filter out all falsy values.
Keep numbers divisible by 5.
Filter strings that are palindromes.
Filter even-length strings.
Filter objects where isActive is true.
Advanced
Filter users by multiple criteria (age > 20 and active).
Keep products where stock > 0 and category = “electronics”.
Filter nested arrays to only include numbers > 50.
Filter strings that contain at least 3 vowels.
Filter out objects with duplicate names.
Keep elements that are prime numbers.
Filter arrays to remove elements appearing more than once.
Filter objects where name starts and ends with the same letter.
Keep numbers whose square is > 100.
Filter words containing both “a” and “e”.
31–50. (Mix numbers, strings, nested arrays, objects, conditions, and combinations)
🔵 REDUCE – 50 Practice Questions
Beginner
Sum all numbers in an array.
Multiply all numbers in an array.
Find the maximum number in an array.
Find the minimum number in an array.
Count total elements in an array.
Concatenate all strings in an array.
Find the sum of even numbers only.
Find the product of odd numbers only.
Count how many times a number appears in an array.
Flatten a 2D array into 1D.
Intermediate
Calculate the average of numbers in an array.
Find the sum of all numbers greater than 10.
Count total vowels in an array of strings.
Combine an array of objects {name} into a single string of names.
Create an array of squares using reduce.
Count how many strings have length > 5.
Convert an array of numbers into an object with {num: frequency}.
Group objects by a property (e.g., category).
Merge multiple arrays into one.
Remove duplicates from an array using reduce.
Advanced
Find the longest string in an array.
Create an object with min and max values from an array.
Count occurrences of each word in an array of strings.
Build a frequency map of letters from an array of words.
Flatten a nested array of objects into a single array.
Calculate the total price from an array of product objects.
Find the top scoring student from an array of student objects.
Create a nested object structure from flat array of objects.
Find the difference between the largest and smallest numbers.
Generate a CSV string from an array of objects.
31–50. (Combine arrays, objects, nested arrays, multiple conditions, frequency maps, sums, averages, etc.)