Begineer level

Create an array of 5 numbers and print each number using a loop.
Find the length of an array.
Access the first and last elements of an array.
Add a number to the end and beginning of an array.
Remove a number from the end and beginning of an array.
Check if an array includes a specific value.
Find the index of a particular element.


##intemediate level
Reverse an array without using .reverse().
Create a new array with all elements doubled (map).
Filter all numbers greater than 10 (filter).
Sum all numbers in an array (reduce).
Merge two arrays into one.
Remove duplicate elements from an array.
Flatten a 2D array into a 1D array.


🔵 Advanced Array Questions
Find the largest and smallest number in an array.
Sort an array of numbers in ascending and descending order.
Create an array of objects (products) and filter all products under a certain price.
Find the average of all numbers in an array.
Find common elements between two arrays.
Count the frequency of each element in an array.
Remove a specific element by value (not index).
Implement your own version of .map or .filter without using built-in methods.
💎 Full-Stack Oriented Array Questions
Given an array of users {name, age}, find all users older than 25.
From an array of products {name, price}, get an array of all product names.
Group an array of objects by a property (e.g., category).
Sort an array of objects by a numeric property (e.g., price).
Implement a shopping cart: add, remove, and calculate total price.